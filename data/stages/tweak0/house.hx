/*
var jump:FlxSprite = new FlxSprite(0, 0);

function postCreate() {
    jump.loadGraphic(Paths.image("stages/tweak0/jumpscare!"));
    jump.setGraphicSize(1280, 720);
    jump.screenCenter();
    jump.alpha = 0.001;
    add(jump).camera=camOther;
}

function onEvent(_) if (_.event.params[0] == 'jumpscare') jump.alpha = 1;*/